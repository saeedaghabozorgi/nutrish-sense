name: photo_upload_app
description: "A new Flutter project."
publish_to: 'none'
version: 3.1.0+1

environment:
  sdk: ^3.11.1

dependencies:
  cloud_firestore: ^6.1.3
  cloud_functions: ^6.0.7
  firebase_auth: ^6.2.0
  firebase_core: ^4.5.0
  firebase_storage: ^13.1.0
  flutter:
    sdk: flutter
  google_fonts: ^8.0.2
  image_picker: ^1.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
